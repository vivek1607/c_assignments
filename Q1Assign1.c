#include<stdio.h>
void main()
{
    int length,breadth,area;
    printf("enter length and breadth of rectangle whose area to be find");
    scanf("%d%d",&length,&breadth);
    area = length*breadth;
    printf("the area of rectangle is : %d",area);

}
